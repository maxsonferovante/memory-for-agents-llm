# MCP OAuth discovery proposal package

## Purpose

Capture the local-stack decision to expose OAuth discovery and resource metadata for the MCP surface so clients that expect `.well-known` endpoints can complete discovery without proxy errors.

## Promotion order

1. Promote `01-mcp-oauth-discovery-adr.md` to document the repo-local OAuth discovery contract for the MCP stack.

## Status

- Package status: ready
- Canonical target:
  - `knowledge/adr/mcp-oauth-discovery.md`
