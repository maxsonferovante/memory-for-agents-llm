# Bidirectional structured memory proposal package

## Purpose

Record the architecture and integration contract for a bidirectional, lossless memory flow between `knowledge/`, the ingestion API, and the MCP read surface.

## Promotion order

1. Promote `01-bidirectional-structured-memory-adr.md` to establish the architectural decision.
2. Promote `02-bidirectional-structured-memory-integration.md` to define the operational contract for ingest, projection, readback, and writeback suggestions.

## Status

- Package status: ready
- Canonical ADR target: `knowledge/adr/bidirectional-structured-memory-mcp.md`
- Canonical integration target: `knowledge/integrations/bidirectional-structured-memory-mcp.md`
